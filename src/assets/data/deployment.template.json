{
    "ref_src":  "${REF_SRC}",
    "DEPLOYMENT_RELEASE": "${DEPLOY_APP_VERSION}",
    "DEPLOYMENT_CFG_REVISION": "${DEPLOY_CONFIG_VERSION}",
    "ENV_CODE_NAME": "${DEPLOY_ENV_CODE_NAME}",
    "RUNTIME_CFG_CONTENT": "${RSA_ENC_B64}"
}